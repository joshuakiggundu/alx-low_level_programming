#include "main.h"
/**
 * _abs - a prototype function
 *
 * Description: always amkeing moves
 * @r: its good
 * Return: (0)
 *
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r *  -1);
}
