include "main.h"

/**
 * main - prints its own name
 *
 * @argc: counts the input
 * @argv: array containimg the strings
 * Return: always(0)
 */
int main(int argc, char *argv[])
{
	_putchar ("%s\n", argv[0]);
	return (0);
}
