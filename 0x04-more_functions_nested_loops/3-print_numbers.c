#include "main.h"
/**
 * print_numbers - prints the numbers and a new line
 *
 * Return: always(0)
 *
 */
void print_numbers(void)
{
	int  i;

	for (i = 48; pr <=57; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
