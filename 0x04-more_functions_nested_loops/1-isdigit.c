#include "main.h"
/**
 * _isdigit -prints the output
 *
 * @c: is an integer
 *
 * Return: (1) or (0)
 *
 */
int _isdigit(int c)
{
	int c = 0;

	if (c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
