#include <stdio.h>

/**
 * main -prints all single digit nuumber of bae ten
 *
 * Return: always(0)
 *
 */
int main(void)
{
	int i = 48;

	while (i <= 57)
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
