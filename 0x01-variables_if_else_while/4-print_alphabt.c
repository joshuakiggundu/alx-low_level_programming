#include <stdio.h>

/**
 * main -print lowercase letters aprt from q en e
 *
 * Return: always (0)
 *
 */
int main(void)
{
	char lower = 97;

	while (lower <= 122)
	{
	if (lower != 101 && 113)
	putchar(lower);
	lower++;
	}
	putchar ('\n');
	return (0);
}
